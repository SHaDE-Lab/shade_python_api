services:
  solweig-jobs:
    build: .
    volumes:
      - data-volume:/app/output
    deploy:
      resources:
        limits:
          memory: 3800m
  flask-app:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - data-volume:/app/output
    deploy:
      resources:
        limits:
          memory: 200m
  
volumes:
  data-volume:

